#include <iostream>
using namespace std;

void contagem_progressiva(int n, int a)
{
    if(a == n){
        cout << a;
        return;
    }
    else{
        cout << a << " ";
        contagem_progressiva(n, a+1);
    }
}

int main()
{
    int n;
    cin >> n;
    contagem_progressiva(n, 1);
}
